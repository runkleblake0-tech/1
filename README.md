# SEB Integration Hub

This is a minimal Vite static site for the SEB Integration Hub used for demonstrations and static deployment.

Local development

1. Set up Node 18+ and npm
2. npm install
3. npm run dev

Build

- npm run build

Deploy

This repository contains a vercel.json for static builds; you can connect it to Vercel or host the dist/ folder on any static host.
